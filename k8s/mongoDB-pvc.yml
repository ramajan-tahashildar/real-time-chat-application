kind: PersistentVolumeClaim
apiVersion: v1 
metadata:
    name: chat-application-mongodb-pvc
    namespace: chat-app
spec:
    storageClassName: local-storage
    accessModes:
        - ReadWriteOnce
    resources:
        requests:
            storage: 5Gi